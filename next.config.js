/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['example.com'], // Reemplaza con el dominio de tus imágenes si es necesario
  },
}

module.exports = nextConfig

